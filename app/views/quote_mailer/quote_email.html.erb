<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>A new quote has been made</h1>
    <p>
      A new quote has been submitted by: <%= @quote.email %><br/>
      For the date <%= @quote.date %>
    </p>

    <p>
      The items requested are:
      <% @quote.item_ids.each do |f| %>
        <p><%= Item.find_by_id(f).name %></p>
      <% end %>
    </p>

    <p>
      Total cost of quote:
      $<%= @quote.item_ids.sum { |a| Item.find_by_id(a).cost } %>
    </p>
    
    <p>
      To visit the app, just follow this link: <%= @url %>.
    </p>
    <h1>
      Please respond to <%= @quote.email %> as soon as possible.
    </h1>
    <p>Also available to call at <%= @quote.phone %></p>
  </body>
</html>