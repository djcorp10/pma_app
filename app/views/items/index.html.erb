<% provide(:title, 'Items') %>
<h1>Items</h1>

<%= will_paginate %>

<ul class="items">
	<div class="accordion" id="accordion2">
  		<%= render @items %>
  	</div>
</ul>

<%= will_paginate %> 